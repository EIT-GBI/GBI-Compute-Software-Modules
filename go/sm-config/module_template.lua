help([[
Go
An open-source programming language and toolchain: the `go` tool (build system,
package manager, test runner), `gofmt`, and the standard library.
https://go.dev/
]])

whatis("Name: go")
whatis("Version: {{{INSTALL_VERSION}}}")
whatis("URL: https://go.dev/")

prepend_path("PATH", "{{{PATH}}}/bin")
